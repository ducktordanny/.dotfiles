# Dotfiles repo

- Commit messages: `Scope: message` (e.g. `NeoVim: Fix LSP root detection`).
- Configs under `config/` and `claude/` are deployed by `setup/apply-config.sh`;
  after changing them, run it (or copy the individual files) to apply.
